import requests
def API_Call(url,file_path):
    #with open(file_path, 'rb') as f:
     #   files = {'image': f}
      #  response = requests.post(url, files=files)
    #return response
    return 1+2
